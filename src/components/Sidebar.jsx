import React from 'react'
export default function Sidebar({ onCreate, search, setSearch, tagFilter, setTagFilter, exportNotes }){ return ( <aside className='w-80 border-r bg-white p-4 flex flex-col gap-4'> <div className='flex items-center justify-between'><h2 className='text-lg font-bold'>Notely</h2><button className='text-sm px-3 py-1 bg-blue-600 text-white rounded' onClick={onCreate}>New</button></div> <input value={search} onChange={e=>setSearch(e.target.value)} placeholder='검색' className='p-2 border rounded' /> <input value={tagFilter} onChange={e=>setTagFilter(e.target.value)} placeholder='태그 필터' className='p-2 border rounded' /> <div className='flex-1' /> <button onClick={()=>{ const txt = exportNotes(); if(!txt) return alert('내보내기 실패'); const blob = new Blob([txt], {type:'application/json'}); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href=url; a.download=`notely_export_${Date.now()}.json`; a.click(); URL.revokeObjectURL(url) }} className='py-2 border rounded'>내보내기</button> </aside> ) }
